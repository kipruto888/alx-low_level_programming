#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 * followed by a new line, except q and e
 * Return: 0
*/
int main(void)
{
	char h;

	h = 'a';
	while (h <= 'z')
{
		if ((h != 'q' && h != 'e') && c <= 'z')
			putchar(h);
		h++;
	}
	putchar('\n);
	return 0;
{

