#include "main.h"
/**
*_pow_recursion - power using recursion
*Qx: The number to be raised
*@y: the power number
* Return: power result
*/
int _pow_recursion(int x, int y)
{
	int result - x;

	if (y < 0)
		return (-1);
	else  if (y == 0)
		return (1);
}
